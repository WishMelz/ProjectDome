const shell = require('shelljs')
const fs = require("fs")
// shell.cd('hengli-yun-mini-app')

// // let aa = shell.exec('git pull')
// let aa = shell.exec('-d zzc')
// const config = require('./config')
// let flg = fs.existsSync(config)
// let aa = shell.which('imgurl')






const sendWs = (shell) => {
    var res = shell
    console.log(res);
}

sendWs(shell.cd('imgurl'))
